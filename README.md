# Currency & Unit Conversion API

A production-ready FastAPI-based API to convert currencies and measurement units. Includes:

- API key authentication  
- SQLModel database  
- Exchange rate caching  
- Real-time currency rates  
- Unit conversions  
- Docker support  
- Render deployment ready  

---

## 🚀 Running Locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
